exports.handler = async (event, context) => {
  //this will update CTMs name field using context.ctm.update similarly can be used to update any standard or custom fields
  await context.ctm.update({
    name: 'hello world'
  });
};
